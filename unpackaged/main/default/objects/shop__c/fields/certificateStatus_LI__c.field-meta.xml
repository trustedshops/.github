<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>certificateStatus_LI__c</fullName>
    <description>Field is necessary for Leading Indicator Reports, since
- a Shop without a certificate can either have certificateStatus &quot;No Audit&quot; or BLANK
- Salesforce Reports can&#39;t include BLANK values into a custom defined bucket.
As soon as either of those two changes, the field can be removed.</description>
    <externalId>false</externalId>
    <formula>IF(ISBLANK(TEXT(certificateStatus__c)),&quot;No Audit&quot;,TEXT(certificateStatus__c))</formula>
    <label>certificateStatus(LI)</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
